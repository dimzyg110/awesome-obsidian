# Dashboards & boards (reference copies)

These are **reference copies** of the dashboards. Their internal `[[wikilinks]]` and `![[embeds]]` resolve by note **basename**, and they use the display names from the ready-to-use vault — not these on-disk filenames:

| Reference file | Vault note name it links to |
| --- | --- |
| `dashboard.md` | `🏠 Dashboard` |
| `crm-dashboard.md` | `👥 CRM Dashboard` |
| `inventory-dashboard.md` | `📦 Inventory Dashboard` |
| `orders-dashboard.md` | `📊 Orders Dashboard` |
| `home.md` | `🏠 Home` |
| `mobile-home.md` | `📱 Mobile Home` |
| `index.md` | `📇 Index` |
| `../boards/projects-board.md` | `Projects Board` |

➡️ **For a working setup, use the assembled vault in [`../starter-vault/`](../starter-vault)** — there each file already carries the name in the right-hand column, so the Home navigation and embeds resolve out of the box.

If you instead copy these loose files into your own vault, rename them to the names above (or update the links) so navigation and `![[…]]` embeds work.
